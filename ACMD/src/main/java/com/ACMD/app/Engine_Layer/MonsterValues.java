package com.ACMD.app.Engine_Layer;

/**
 * Questa classe in realta è una struct serve solo a memorizzare i valori di un mostro
 */
public class MonsterValues {
    public short health, maxHealth;
    public byte damage, armor, level;
    public byte healthMul, armorMul, damageMul;
}
